CXX = clang++
CXXFLAGS = `libpng-config --cflags` -O1
LDFLAGS = `libpng-config --ldflags` -lfmt
OUTPUT = pic2lcd

: foreach *.cpp |> $(CXX) -c %f -o %o $(CXXFLAGS) |> %B.o
: *.o |> $(CXX) %f -o %o $(LDFLAGS) |> $(OUTPUT)
